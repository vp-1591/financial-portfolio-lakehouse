---
topic: Rename CDC->events and demo->staging in one PR
slug: rename-cdc-events-demo-staging
updated: 2026-08-19T09:18
---

- (note) Input: issue #131 (CDC is a misnomer for the broker activity event log; rename to 'events') plus user direction to rename remaining 'demo' AWS resources/function names to 'staging'; both need migrations, land in one PR.
- (capability) CAP-1: codebase names the event layer 'events' not 'CDC' - files, functions, constants, config paths, CLI subcommands, report sections, quality checks, tables; success: grep -rni 'cdc' pipeline/ tests/ (excl. ADRs) returns zero hits and full test suite green.
- (capability) CAP-2: staging AWS environment named 'staging' everywhere - terraform resource identifiers, live AWS resource names (S3 bucket, IAM user/policies/roles, VPC, SG, state machine), SSM param paths, data prefix, env label; success: 'demo' absent from staging terraform naming and from applied AWS resource names.
- (capability) CAP-3: migrations preserve existing encrypted Delta data across renames - table renames ({broker}_cdc->{broker}_events, cdc_events->events) and bucket/prefix rename (pipeline_demo->pipeline_staging); success: post-migration row counts identical to pre-migration via pipeline.run query --decrypt.
- (capability) CAP-4: both renames delivered as one PR with migrations, updated tests, and a new ADR recording the event-layer rename; success: PR opens containing both rename tracks, all three checks pass (ruff, pyright, pytest), migrations applied to staging.
- (constraint) Old ADRs are permanent records: never rewritten to 'events'; the rename supersedes the naming decision only, not the decisions themselves.
- (constraint) event_* column names are already correct and are out of scope for change.
- (constraint) Migrations follow the existing pattern (pipeline/migrations/): idempotent (exit 0 on absent/already-migrated), raise on genuine failures, run manually BEFORE deploying code referencing the new names, use pipeline.run CLI not manual DeltaTable construction.
- (constraint) S3 bucket name is globally unique so investment-portfolio-pipeline-demo->investment-portfolio-pipeline-staging means new bucket + full encrypted-data copy + secret/storage re-point, not an in-place rename.
- (constraint) SSM paths /portfolio/demo/* -> /portfolio/staging/* requires creating new parameters with the same secret values, updating deploy workflow references, and retiring the demo-prefixed ones.
- (constraint) Terraform renames use planned state migration (moved blocks) or deliberate destroy/recreate; never apply prod terraform.
- (non-goal) Out of scope: rewriting old ADRs (keep original wording), changing event_* column names, renaming prod-environment resources, renaming docker/MinIO local-mode resource names except where shared naming code forces consistency.
- (assumption) Assumed 'function names' in the user's phrasing means Step Function (state machine) names plus the Python fetch_cdc / dedup_cdc_events functions - all covered by the two rename tracks.
- (assumption) Assumed the residual 'demo' naming in staging infra - terraform module var 'demo', env_label='demo', MODE_TO_ENV_LABEL {'staging':'demo'} - is part of the rename to 'staging', including the var value flip in prod/main.tf (demo=false -> staging=false).
- (question) Open Q: should the physical S3 bucket name change (global rename requiring data copy) or only logical references/prefixes be renamed while the bucket stays? User intent says rename resources, but the copy cost is real.
- (question) Open Q: what happens to the retired /portfolio/demo/* SSM parameters after /portfolio/staging/* is live - immediate deletion or a grace period?
- (question) Open Q: does the CDC rename extend to pipeline/analytics/cdc_tables.py and DQ/quality-check config keys that reference cdc_events, or only the tables/functions/files the issue lists?
- (event) Pass 1 (coherence): kernel drafted against Spec Law 1-6+8; each capability has intent+success, intents are WHAT, constraints rule out options, IDs stable CAP-1..CAP-4.
- (event) Pass 2 (preservation): all load-bearing claims from issue #131 and the demo->staging inventory landed in SPEC.md or companions; wrapper ceremony (issue metadata, historical ADR citations) dropped as wrapper-only.
- (decision) Decided: staging S3 prefix is removed entirely (empty prefix), not renamed to pipeline_staging - buckets already isolate environments (ADR 0038/0039), so the pipeline/pipeline_demo prefix is redundant; S3Backend and query._discover_tables_s3 already support empty prefix, terraform s3_prefix var, ECS S3_PREFIX env, xtb_staging_prefix, and tests flip accordingly; prod pipeline prefix stays (out of scope, needs prod apply - deferred follow-up).
- (direction) Q3 answered (user): the CDC rename extends to pipeline/analytics/cdc_tables.py and DQ/quality-check config keys; success bar is grep -rni 'cdc' pipeline/ tests/ (excluding ADRs) returning zero results.
- (decision) Q1 answered (user): physical S3 bucket rename (investment-portfolio-pipeline-demo -> investment-portfolio-pipeline-staging = new bucket + full encrypted-data copy); keep-bucket-and-alias option dropped.
- (decision) Q2 answered (user): retire /portfolio/demo/* SSM params immediately, but user sets /portfolio/staging/* values FIRST (before terraform apply - apply references the new paths); ordering: set values -> terraform apply -> retire demo paths immediately after.
- (decision) Demo grep-zero bar adopted (user): grep -rni 'demo' over non-ADR sources returns zero, with explicit carve-outs: broker product-tier 'demo' (Trading 212 demo.trading212.com URL + DEMO_BASE_URL/_DEMO_BASE_URL constants; IBKR demo-account deposit injection _inject_demo_deposit/_DEMO_INITIAL_DEPOSIT_AMOUNT and transform is_demo param), vendored third-party docs (docs/_vendor/), historical planning records (docs/adr/, docs/roadmaps/, docs/xtb/), and spec/planning artifacts (bmad-output/). secrets.is_demo() is the staging-mode predicate and renames to is_staging(); ibkr transform's is_demo param is broker-tier and stays.
- (event) Spec update validated: Pass 1 (coherence) - CAP-2 success now a concrete grep bar with path carve-outs + broker-tier allow-list, CAP-3 success covers SSM swap, new SSM-sequencing constraint, non-goals extended with broker/vendored carve-outs, IDs stable CAP-1..4; Pass 2 (preservation) - Q1/Q2/demo-bar decisions all landed in SPEC.md or rename-plan.md.
- (decision) Stale planning doc docs/xtb/xtb_overhaul_plan.md is REMOVED (user): it was a subagent-implementation blueprint, not a decision record; its 22 binding decisions D1-D22 live in ADR 0108 (partially superseded by ADR 0110), so no decision content is lost. docs/xtb/ is dropped from the demo grep carve-outs.
- (decision) Final demo grep allow-list confirmed (user): docs/adr/, docs/_vendor/, docs/roadmaps/, _bmad-output/ plus the Trading 212 paper-trading API tier (demo.trading212.com URL + DEMO_BASE_URL/_DEMO_BASE_URL). Nothing else survives the grep bar.
- (decision) Stale planning doc docs/xtb/xtb_overhaul_plan.md is REMOVED (user): it was a subagent-implementation blueprint, not a decision record; its 22 binding decisions D1-D22 live in ADR 0108 (partially superseded by ADR 0110), so no decision content is lost. docs/xtb/ is dropped from the demo grep carve-outs.
- (decision) Final demo grep allow-list confirmed (user): docs/adr/, docs/_vendor/, docs/roadmaps/, _bmad-output/ plus the Trading 212 paper-trading API tier (demo.trading212.com URL + DEMO_BASE_URL/_DEMO_BASE_URL). Nothing else survives the grep bar.
- (decision) IBKR 'demo' is project-stale terminology (user, verified): IBKR's account class here is the PAPER TRADING account (DU prefix, 1M virtual money, persistent Flex history) - IBKR's own 'demo' (edemo/fdemo) is a transient daily-reset account that does not match. Renames: transform is_demo param -> is_paper, _inject_demo_deposit -> _inject_paper_deposit, _DEMO_INITIAL_DEPOSIT_AMOUNT -> _PAPER_INITIAL_DEPOSIT_AMOUNT, 'demo account' text -> 'paper account'; ibkr connector passes is_staging() into is_paper. T212 stays whitelisted (its tier is genuinely 'demo').
- (event) Spec update validated: Pass 1 (coherence) - CAP-2 success/allow-list/non-goals consistent after dropping docs/xtb carve-out and moving IBKR to paper rename; Pass 2 (preservation) - xtb-plan removal, whitelist, and IBKR-paper decisions all landed in SPEC.md or rename-plan.md.
- (event) (arch) Run started: bmad-architecture create, feature altitude, build-substrate. Spec package is the input; spine lands as a companion of SPEC.md in the same workspace (no parent spine; project ADRs 0038/0039/0090/0092/0025/0065 cited as binding context, not inherited invariants).
- (decision) (arch AD-1) The rename map (rename-plan.md old->new tables) is the single source of truth for every renamed symbol/table/resource; binds both tracks, tests, docs. Prevents split-track drift choosing synonyms (transactions vs events) or inventing names outside the map. Rule: new names come verbatim from the map New column; any extension is a spec-level decision, not an implementation choice; every Old-column token outside docs/adr/ must move to New.
- (decision) (arch AD-2) Rename target is decided by semantic ROLE, not by the token string: (a) pipeline environment mode -> staging; (b) broker product tier -> the broker's own vocabulary (Trading 212 keeps 'demo' - its tier name; IBKR -> 'paper'); (c) stale local jargon -> follow the map. Fixes the is_demo split: secrets.is_demo() (mode predicate) -> is_staging(), ibkr transform is_demo (tier param) -> is_paper; MODE_TO_ENV_LABEL becomes identity so the env label is derived, never hardcoded. Prevents two units uniform-renaming the same token to incompatible targets.
- (decision) (arch AD-3) The grep-zero sweeps are the closed enforcement gate and every unit's definition of done. 'cdc' zero over pipeline/ tests/ (excl docs/adr/); 'demo' zero over pipeline/ tests/ terraform/ .github/ docs/ README.md outside the fixed allow-list (docs/adr/, docs/_vendor/, docs/roadmaps/, _bmad-output/, T212 tier tokens demo.trading212.com + DEMO_BASE_URL/_DEMO_BASE_URL). Prevents residual tokens surviving unnoticed - the rename's core failure mode. Rule: allow-list is closed, no additions without a spec decision; a unit is done only when its sweep returns zero.
- (decision) (arch AD-3) Migration-first, deploy-last: strict sequence for data-safe rename. Binds migration scripts, deploy workflow, run.py. Prevents renamed code running against unmigrated tables/params. Rule: (1) user sets /portfolio/staging/* values = same secret values copied, never regenerated; (2) terraform apply to staging (moved blocks, never prod); (3) retire /portfolio/demo/* immediately after live; (4) B1 data copy to new bucket root (no prefix); (5) A1 table renames {broker}_cdc->{broker}_events, cdc_events->events; (6) verify identical row counts via pipeline.run query --decrypt; (7) deploy renamed code last. Migrations: idempotent (exit 0 on absent/already-migrated, raise on genuine failure), run manually pre-deploy per env, never hand-built DeltaTable.
- (decision) (arch AD-4) The immutables this rename must not touch. Binds all units. Prevents rename overshoot. Rule: docs/adr/ content never rewritten to events/staging (history stays); event_* columns unchanged; prod env untouched (no prod terraform apply; prod pipeline prefix stays); T212 tier tokens are the sole surviving 'demo'; test fixture strings fall under the map and bar.
- (decision) (arch AD-5) Both tracks ship as ONE PR (CAP-4): Track A (events) + Track B (staging) + migrations A1/B1-B3 + updated tests + new ADR recorded via manage-adr after merge. Prevents partial merge that breaks a sweep or interlocking migrations. Rule: single PR; merge only after ruff/pyright/pytest green and migrations applied with counts verified.
- (assumption) (arch) No parent architecture spine exists - this spine stands alone at feature altitude; the ~35 prior ADRs are cited as constraints (ADR 0038/0039 bucket isolation, ADR 0090 --mode flag, ADR 0092 _DEMO suffix removal, ADR 0065 CDC analytics) not re-derived.
- (question) (arch) rename-plan.md cites migrate_snapshot_schema_unify.py as the migration pattern to mirror, but that file was REMOVED (commit e2c8d84); the live pattern is pipeline/migrations/migrate_xtb_purge_legacy_raw.py. Migration author should mirror the live one; flag for bmad-spec to correct the plan.
- (event) (arch) Reviewer gate ran (lint + 3 parallel lenses: reality-check, adversarial divergence, input reconcile). Applied clear fixes: AD-1 enumerates real Track B code owners + _consolidate_cdc/_normalize_cdc helpers + residual *cdc*->*_events pattern + onboarding artifacts; AD-2 adds role (d) data-embedded sentinels (flex_cdc needs data rewrite, not just code rename); AD-3 exempts historical-reference artifacts (migration scripts, moved/state-mv blocks) from the token bars; AD-4 reorders B1 copy BEFORE the bucket-repointing apply, forbids force_destroy, adds IAM access-key re-export, adds sentinel value rewrite to A1; AD-5 adds prod/main.tf staging=false flip as the one in-scope prod edit; AD-6 notes conditional demo->staging ADR; conventions/seed/deferred corrected (deploy-staging.yml has no SSM refs; xtb_cdc precedent reworded).
- (question) (arch) Spec ratifications requested by the gate: (1) AD-3 historical-reference exemption extends the closed carve-out list; (2) flex_cdc sentinel route (rename + A1 data rewrite) vs CAP-1 exemption; (3) CAP-1 bar scope omits docs/ but docs/table-lineage.md + two docs/ruff-*.md carry cdc - extend bar to docs/ or narrow SPEC success language. Also rename-plan.md CLI row is mislabeled (no argparse subcommands - real surfaces are run.py _consolidate_cdc/_normalize_cdc + error strings) and its migrate_snapshot_schema_unify.py reference is stale (removed e2c8d84) - bmad-spec should correct both.
- (event) (arch) spine finalized: ARCHITECTURE-SPINE.md (map-locked lockstep rename, feature altitude, AD-1..AD-6) landed as companion of SPEC.md; status final.
