---
topic: PR #148 (remove-hardcoded-connectors): lock the goal and the consolidation-scope invariant for the XTB regression before choosing the fix
updated: 2026-08-20T12:27
---

- (note) Sources: PR #148 diff (94cdbac), /code-review findings (3), audit tmp/PROGRESS.md, plan tmp/plan-fix-pr148.md, ADRs 0091/0107/0108/0110, tfvars staging+prod.
- (direction by user) Goal of this spec: define background, current conditions, goal and why for PR #148 before choosing any solution.
- (direction) PR intent (title 'Remove hardcoded connector requirements'): connector-agnostic pipeline, per-trigger connector set declared by config (tfvars) and execution input, not hardcoded Python gate lists.
- (decision) Invariant: no silent data loss - a broker already in silver must keep appearing in consolidated events and derived analytics on later runs that did not fetch it.
- (event) Regression (finding 1): scheduled/manual SFN runs pass DEFAULT_CONNECTORS=[ibkr,trading212] so consolidate_events skips xtb_events and overwrite-mode rewrite drops XTB from events; dividend_income/interest_income rebuilt from events lose XTB until next upload.
- (event) Internal inconsistency: cmd_consolidate (run.py:294) still iterates registry all_connectors() so holdings keep XTB while events do not.
- (capability) CAP-1: event consolidation covers every broker with silver data, not only the brokers the run fetched. Success: seed xtb_events, run consolidate_events(['ibkr','trading212']), assert merged events still contain the xtb rows.
- (capability) CAP-2: per-trigger connector sets are declared, not gated in Python. Success: no required/optional broker gate lists exist in pipeline code; editing schedule_connectors/file_arrival_connectors changes the per-trigger set with no code change.
- (capability) CAP-3: missing or empty enabled event tables are WARN, not FAIL. Success: run_validation(connectors=[...]) returns WARN (exit 0) for a missing/empty enabled x_events table.
- (constraint) C1 (ADR 0091 #6 + ADR 0110): XTB fetch+transform runs only on file-arrival; scheduled/CI runs must not launch a no-op run-connector xtb task. This rules out re-adding xtb to the scheduled set.
- (constraint) C2 (ADR 0110 §Decision): consolidation+analytics read ALL registered silver tables via the registry (all_connectors()), independent of the scheduled-connectors list.
- (constraint) C3 (ADR 0107): broker lists live in committed auto-tfvars (schedule_connectors, file_arrival_connectors); orchestrator Terraform passes them into the SFN input.
- (constraint) C4: consolidate_events writes normalized/events with mode=overwrite (no merge), so the consolidated set must be complete before the write.
- (constraint) C5 (adr-workflow): reversing or altering an active ADR requires superseding via manage-adr, never hand-editing the ADR file.
- (decision) Non-goal NG1: XTB does not return to the scheduled connector set; no scheduled no-op XTB task.
- (decision) Non-goal NG2: file_arrival_connectors stays [ibkr, trading212, xtb]; file-arrival trigger behavior unchanged.
- (decision) Non-goal NG3: no re-architecture of the connector registry, broker naming, or the tfvars schema.
- (decision) Non-goal NG4: no schedule/cadence changes (staging scheduled=false; prod monthly cron(0 6 1 * ? *)).
- (decision) Non-goal NG5: DEFAULT_CONNECTORS (pipeline/sfn.py) stays as the manual/CI trigger default; not derived from config in this work.
- (assumption) The fix keeps ADR 0110 active and does not re-hardcode XTB - recorded from the user's stated unease with Fix 1 and the audit; this binds the HOW but was not yet confirmed as the final direction.
- (question) Q1: Is the all-silver consolidation rule the permanent general semantics (any future broker whose refresh decouples from the schedule is covered), or is preserving it for the current brokers enough?
- (question) Q2: CAP-1 + C1 constrain the fix to registry-based consolidation over all silver while keeping per-trigger fetch/validate lists - confirm this is the goal the fix must serve.
- (note) Companion: trigger-connector-matrix.md - per-trigger fetch/consolidate/validate connectors and cadence table.
- (event) Draft complete: SPEC.md + trigger-connector-matrix.md derived from 23 memlog entries.
- (event) Self-validate Pass 1 (Coherence): OK - Spec Law rules 1-6 and 8 satisfied; each capability has intent+success; every constraint rules something out; assumptions/questions recorded; no invented content.
- (event) Self-validate Pass 2 (Preservation): OK - every load-bearing source claim landed in SPEC.md or trigger-connector-matrix.md. Wrapper-only content dropped: /code-review finding verbatim text, test line numbers, plan/commit steps, registry-restore trade-off detail (HOW, deferred to the plan).
- (decision) Q1 resolved (user: Yes): all-silver consolidation is the permanent general semantics - any future broker whose refresh decouples from the schedule is covered by consolidation via the registry
- (decision) Q2 resolved (user: Yes): the goal is registry-based consolidation over all silver while keeping per-trigger fetch/validate lists - Fix 1 (re-adding xtb to scheduled sets) is excluded
- (event) Spec finalized: Q1+Q2 answered, no open questions remain; ready for plan
- (event) Self-validate Pass 1 (Coherence): OK - Spec Law 1-6,8 satisfied; CAPs have intent+success; C1-C5 each rule out a real option; Open Questions section removed (Q1/Q2 answered); assumption promoted to confirmed Decisions D1-D3
- (event) Self-validate Pass 2 (Preservation): OK - every load-bearing memlog claim landed in SPEC.md or trigger-connector-matrix.md; wrapper-only content dropped: /code-review finding verbatim text, test line numbers, plan/commit steps
