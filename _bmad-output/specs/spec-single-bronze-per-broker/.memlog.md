---
topic: Adopt a single bronze (raw) table per broker as the project convention
updated: 2026-08-20T15:14
---

- (decision) Source intent: GitHub issue #144 (Adopt single bronze table per broker as convention). User direction: the idea stands, but issue details are outdated and the suggested 5-PR sequence is an over-split — deliver this as ONE pull request.
- (note) Current state verified against main (2026-08-20): 'cdc' naming already renamed to 'events' project-wide (ADR 0113, migrate_cdc_to_events.py); ADR 0108 superseded by ADR 0110 with D17 shared-bronze carried forward; connectors live in pipeline/connectors/{ibkr,trading212,xtb}/ with per-broker connector.py/fetch.py/transform.py plus a registry (PR #148).
- (capability) CAP-1 One raw Delta table per broker, source column discriminates data kind. intent: Operator stores every payload a broker returns (snapshot, positions, events) in a single raw table raw/{broker}, discriminated by source, with no per-layer raw plumbing. success: After migration raw/{ibkr,trading212,xtb} each hold rows from both fetch kinds; no {broker}_snapshot or {broker}_events raw paths remain; SELECT DISTINCT source returns the expected values per broker.
- (capability) CAP-2 XTB is no longer a special case in fetch/transform. intent: Developer runs fetch and transform for any broker through one generic path, with no broker-name branch, no raw-layer override attribute, no stub methods. success: name==xtb branch in fetch_connector deleted; events_raw_layer attribute removed; transform_connector reads a single raw path; XTB conforms to the shared protocol; full test suite passes.
- (capability) CAP-3 Rows route to the correct silver transform after the merge. intent: System sends each raw row to its silver table — snapshots to normalized/{broker}_snapshot, events to normalized/{broker}_events — based on source. success: Regression tests prove T212 /equity/positions list payloads never reach the events silver; IBKR and T212 events transforms include-filter only event sources; both silver tables per broker are unchanged in schema and contents.
- (constraint) Bronze only. Silver stays two normalized tables per broker (different schemas, consumers, freshness gates). Do NOT merge silver.
- (constraint) API calls do not merge — only the write target. IBKR still issues distinct Flex queries; T212 still calls its 5 endpoints (account summary, positions, orders, dividends, transactions). Preserve write-what-succeeded, surface-the-failure isolation (per-endpoint try/except stays).
- (constraint) Merge migration is idempotent, dedup-keyed on (broker, source, payload_hash), supports --dry-run, and purges the orphaned xtb_events raw table (never written since ADR 0108; docs/table-lineage.md currently draws a false edge to it).
- (constraint) Migration must deploy before renamed-path transforms run, or transform_connector silently skips and quality gates flag missing tables.
- (constraint) filter_latest_snapshot stays per-source (a future global max would silently drop streams) — add a regression guard.
- (constraint) T212 all-events-endpoints-empty RuntimeError (fetch.py:121) must survive the merge — required-non-empty behavior depends on it.
- (constraint) Raw table is named {broker} under raw/ (query alias {broker}_raw via auto-discovery). Do not keep the misleading {broker}_snapshot name XTB currently uses.
- (constraint) Explicit source gates: events transforms include-filter event sources (IBKR flex_events; T212 /orders|/dividends|/transactions) before payload unwrapping — never substring luck.
- (constraint) Non-goal: no single global bronze across brokers (issue option C rejected — broker discrimination would move into every query/filter and blur failure isolation).
- (constraint) Non-goal: no merge or collapse of per-broker API fetches (IBKR 2 Flex queries, T212 5 endpoints stay separate fetches).
- (constraint) Non-goal: no change to RAW_SCHEMA fields or payload formats.
- (assumption) Issue details verified against main (2026-08-20) rather than taken at face value; 'cdc' terms in the issue map to today's 'events' naming; IBKR transform_events already carries an explicit source gate.
- (assumption) Single-PR delivery reflects explicit user direction; the issue's 5-PR sequence is superseded.
- (direction) Success signal: one PR lands — the migration yields exactly one raw Delta table per broker (raw/{broker}, alias {broker}_raw) in staging and prod; XTB special-casing is gone from the code; T212 positions provably cannot reach the events silver; both silver tables per broker keep pre-merge contents; ruff, pyright, pytest all pass.
- (event) Self-validate pass 1 (coherence): SPEC satisfies Spec Law rules 1-6 and 8 — every capability has intent+success, intents are WHAT not HOW, constraints rule things out, non-goals explicit, success signal testable, lean prose. No fixes needed.
- (event) Self-validate pass 2 (preservation): every load-bearing claim in issue #144 lands in SPEC.md or architecture-diagrams.md; rejected option C and the decision-matrix verdict recorded as non-goal + this log. Wrapper-only drops (on record): the pros/cons narrative, the A/B/C scoring table, and the superseded 5-PR sequence.
