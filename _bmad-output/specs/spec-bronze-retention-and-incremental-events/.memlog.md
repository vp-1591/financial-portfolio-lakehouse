---
topic: Bounded bronze retention and incremental normalized events
goal: Preserve broker data while removing unnecessary raw duplicates and stale freshness semantics
scope: bronze retention, incremental normalized events, current-fetch freshness, delta vacuum policy
purpose: architecture spine for the bounded bronze retention + incremental events epic
altitude: epic
updated: 2026-08-23T09:15
---

- (direction) The user selected one broker-scoped bronze table with broker-specific retention and deduplication instead of a separate observability table.
- (decision) Raw retention keeps the latest XTB report per account, the latest Trading 212 response per endpoint, and all IBKR event coverage needed to avoid Flex query-window loss.
- (decision) Normalized IBKR events must be dedup-append by event_id rather than overwrite so events missing from a later Flex response remain available.
- (constraint) XTB account_id is not a RAW_SCHEMA field; current code derives it from source_file during silver transformation, so any raw retention key must preserve that boundary or add an explicit ingestion identity design.
- (decision) Delta deleted-file retention defaults to one week and Delta log retention defaults to 30 days; VACUUM is not automatic, so defaults alone do not implement raw retention.
- (capability) Broker-specific bronze retention. intent: Keep only the raw payload history required by each broker's data semantics while retaining one raw table per broker. success: XTB retains the latest report per account, Trading 212 retains the latest response per endpoint, and IBKR retains sufficient event payloads to cover normalized event history.
- (capability) Incremental normalized events. intent: Preserve existing normalized events while adding or updating events from new broker fetches instead of replacing the entire events table. success: IBKR events absent from the latest Flex response remain in normalized storage, repeated event_id values resolve to the latest version, and no event is lost because the query window moved.
- (capability) Fresh fetch timestamps. intent: Make normalized freshness reflect the current successful fetch even when the raw payload is deduplicated. success: identical repeated fetches do not produce stale freshness warnings solely because their payload hash already exists.
- (constraint) Trading 212 and XTB assumptions are explicit: Trading 212 responses are complete for their endpoints; XTB account identity is the logical retention boundary; broker corrections and response truncation are out of scope unless tests or source contracts contradict these assumptions.
- (constraint) A Delta table's default seven-day deleted-file retention applies only when VACUUM removes logically deleted files; append-only writes do not delete old rows, and the default Delta log retention is 30 days.
- (question) How will the implementation derive or persist an XTB account retention key without adding account_id to RAW_SCHEMA, which ADR 0047 intentionally keeps out of bronze?
- (question) Does issue #155 require retaining the existing Trading 212 handoff optimization, replacing it with incremental event writes, or adding per-layer filtered reads for IBKR/XTB? Confirm the issue's exact acceptance criteria before implementation.
- (question) Should bronze retention be implemented as physical row deletion/compaction, or as write-time admission of only the broker-specific latest rows followed by a scheduled VACUUM policy?
- (assumption) Issue #155 is related because it extends the current handoff design toward per-layer handoff, filtered reads, and bounded dedup scans; it can share this PR only if its acceptance criteria remain within the retention and incremental-events contract.
- (note) Issue #157 is addressed by removing the false stale-fetched_at path: normalized event history is append-preserved while current fetch metadata or current-fetch transformation supplies freshness.
- (event) Activation used express distillation because the user supplied a complete decision set; unresolved implementation choices remain open questions.
- (event) Self-validate pass 1 coherence: the spec has four capabilities with intent and success, explicit constraints and non-goals, testable success signals, and lean prose; open questions preserve unresolved XTB identity, retention mechanics, and issue #155 scope.
- (event) Self-validate pass 2 preservation: all user decisions about one bronze table, broker-specific retention, IBKR event preservation, Delta defaults, handoff review, and issue mappings appear in SPEC.md or retention-and-events-contract.md; web verification of issue #155 was unavailable, so its exact acceptance criteria remain explicitly open.
- (direction) IBKR raw retention does not need multiple historical reports: incremental normalized event tables for every broker preserve retained event history, so IBKR raw keeps only the latest report required by its source policy.
- (event) Self-validate pass 1 coherence after correction: CAP-1 now requires only latest IBKR raw report per source, CAP-2 and the constraint preserve incremental normalized events for every broker, and no capability IDs or testable success signals changed.
- (event) Self-validate pass 2 preservation after correction: the user direction that multiple IBKR raw reports are unnecessary because normalized event tables are incremental appears in SPEC.md and retention-and-events-contract.md; the prior raw-event-coverage wording is superseded.
- (direction) The primary Why includes major read and dedup speed optimization and bounded raw storage: each new fetch must not make raw reads, deduplication, and storage grow indefinitely.
- (event) Self-validate pass 1 coherence after motivation correction: Why leads with bounded storage and read/deduplication speed, CAP-1 carries those outcomes with testable bounded-growth language, and the existing broker/event guarantees remain intact.
- (event) Self-validate pass 2 preservation after motivation correction: the user's primary motivation for major read/deduplication speed optimization and preventing indefinite per-fetch storage growth appears in SPEC.md and the Delta retention companion.
- (decision) Raw retention maintenance runs before every fetch: remove or compact out-of-policy raw data, then run VACUUM so storage and subsequent read/deduplication work stay bounded.
- (event) Self-validate pass 1 coherence after retention decision: CAP-4 now explicitly requires removal or compaction followed by VACUUM before every fetch, and the resolved maintenance choice is no longer an open question.
- (event) Self-validate pass 2 preservation after retention decision: the user's requirement that VACUUM run before every fetch appears in SPEC.md and retention-and-events-contract.md.
- (decision) Raw schema adds nullable account_id and removes source_file: XTB fetch populates account_id from the report filename, IBKR and Trading 212 store NULL, XTB transform groups by raw account_id, and rows with NULL account_id recover it by parsing the raw payload; fetched_at plus payload_hash supplies any required deterministic tie-break.
- (event) Self-validate pass 1 coherence after raw account identity decision: the account_id open question is resolved, the schema boundary is explicit, null handling avoids cross-broker grouping, and the capability contract remains testable.
- (event) Self-validate pass 2 preservation after raw account identity decision: the user's decision to add nullable raw account_id and remove source_file, including XTB filename derivation and null-payload fallback, appears in SPEC.md and retention-and-events-contract.md.
- (capability) Single bronze read for both normalized outputs. intent: Reuse one broker bronze read and decoded result to populate both snapshot and event tables. success: Each broker run reads its bronze table once, routes the shared result to both transforms, and does not re-read or reparse the same bronze payload separately for the downstream tables.
- (event) Self-validate pass 1 coherence after issue-155 decision: CAP-5 has intent and success, explicitly requires one shared bronze read for both normalized outputs, and removes issue #155 from open questions.
- (event) Self-validate pass 2 preservation after issue-155 decision: the user's requirement to read bronze once and reuse it for snapshot and event outputs appears in SPEC.md and retention-and-events-contract.md.
- (question) Should the T212 in-memory encrypted fetch handoff be removed now that retention bounds bronze reads, or retained to avoid one bounded bronze read and its network roundtrip? The choice affects code complexity, latency, and current-fetch semantics but not the single-read-for-both-outputs contract.
- (event) Self-validate pass 1 coherence after handoff matrix: the T212 handoff choice is explicitly unresolved, CAP-5 remains independent and requires one shared bronze result for both outputs, and the matrix provides a measurable decision rule.
- (event) Self-validate pass 2 preservation after handoff matrix: the user's two alternatives—remove complexity or retain the handoff to avoid one bounded/network bronze read—appear in handoff-decision-matrix.md and are referenced from SPEC.md.
- (note) The handoff decision matrix now marks the stronger option per dimension with emojis and uses a tie or measurement marker where neither option clearly wins.
- (decision) Remove the Trading 212 in-memory fetch handoff for a measured simplification experiment; compare memory peak and runtime with the existing handoff measurements, and restore the handoff if the regression is material.
- (event) Self-validate pass 1 coherence after handoff decision: the previously open T212 choice is resolved as a reversible measurement experiment, with explicit baseline comparison and material-regression rollback criteria.
- (event) Self-validate pass 2 preservation after handoff decision: the user's decision to remove the handoff, measure memory/time, and restore it if needed appears in SPEC.md, the retention companion, and the decision matrix.
- (event) spine run started: coaching path; deliverable is spine + interactive HTML deck; parent feature spine single-bronze-per-broker inherited as binding (AD-1..AD-7, paradigm medallion per-broker single bronze port)
- (decision) VACUUM uses the Delta 7-day default tombstone retention (retention_hours omitted, enforce_retention_duration stays True): out-of-policy rows are removed logically by delete/rewrite so reads and dedup scans are bounded immediately, and VACUUM before each fetch reclaims physical bytes on a rolling 7-day schedule. Per-run retention_hours≈0 rejected as over-aggressive and off-spec.
- (decision) Events merge (CAP-2): native DeltaTable.merge on the per-broker event identity (event_id + broker fields); source is the in-batch pre-deduped normalized rows (dedup_events); whenMatchedUpdate only when incoming fetched_at is newer, whenNotMatchedInsert; events absent from the current response are never deleted (append-preserving). Read-merge-write rejected: rewrites the whole history each run.
- (decision) Freshness (CAP-3): run-aware. The run passes per-broker last-successful-fetch timestamps into the DQ freshness pass; a table passes when its broker was fetched successfully within the window even if no payload changed; falls back to table max(fetched_at) when DQ runs standalone. No new metadata table.
- (decision) RAW_SCHEMA migration backfills XTB account_id from source_file before source_file is dropped, so retention grouping and transform grouping are correct from day one; the transform's payload-parse recovery stays as the mandated fallback for genuinely null rows.
- (decision) Bronze retention mechanism: write-time merge-on-key. Every broker raw write is DeltaTable.merge on the broker retention key (XTB account_id; Trading 212/IBKR source/endpoint): matched rows replaced by the current fetch row, unmatched inserted, keys absent from the current fetch untouched. In-batch (source, payload_hash) dedup stays; dedup_raw's cross-run full-table scan is deleted (removes the read/dedup work the feature exists to kill). VACUUM 7-day default runs per broker run; tombstone created by the merge means vacuum lands after the write, behaviorally immaterial at a 7-day threshold. NULL retention keys (unparseable XTB account) fall back to append + in-batch dedup, documented as the spec's acknowledged filename risk.
- (event) spine distilled: ARCHITECTURE-SPINE.md with 8 ADs (write-time merge-on-key retention, RAW_SCHEMA account_id evolution superseding ADR 0047, 7-day VACUUM policy, events append-preserving merge, run-aware freshness, single bronze read, migration deploy gate, T212 handoff measured removal); lint clean (only intentional {broker} tokens); reviewer gate dispatched (rubric, adversarial, web-reality, spec reconcile)
- (event) Reviewer gate resolved (4/4 completed: reconcile, rubric, adversarial, web-reality): rubric REVISE items 1H/3M/2L all applied; adversarial 5 divergences all resolved; web-reality PASS with dry_run=False vacuum fix applied; reconcile gaps closed. Applied fixes include: vacuum dry_run=False mandate (deltalake 1.6.0 dry_run defaults True), events write cite corrected to run.py:301-306, AD-4 identity pinned to per-broker dedup subsets (IBKR [event_id], T212 [event_type,event_id], XTB [event_type,event_id,account_id]), NULL retention keys append-never-merge, T212 merge key = endpoint base (pagination suffix stripped), ADR 0116 no-merge constraint registered as superseded in Conflicts surfaced, AD-5 multi-broker freshness mapping (consolidated tables → MAX over brokers), AD-7 filename-only backfill, ops envelope (per-connector ECS task vacuums its own raw/ broker; silver never vacuumed), tests row guards (dry_run=False removal guard, T212 paginated-merge guard), two non-goals added.
- (event) spine status set to final; lint re-run confirms 0 HIGH (only 10 intentional {broker} tokens); architecture-deck.html updated with all reviewer fixes (vacuum dry_run=False card, ADR-0116 supersede surfaced, 2 non-goals).
- (note) bmad-spec update run: adopt the finalized architecture spine (ARCHITECTURE-SPINE.md, 8 ADs, status final) as a companion so downstream skills can cite stable AD IDs; the spine itself is owned by bmad-architecture and is not edited here.
- (decision) Open question: how to derive an XTB account retention key without adding account_id to RAW_SCHEMA (ADR 0047 keeps it out). Resolved by the architecture spine: RAW_SCHEMA adds nullable account_id (AD-2, superseding ADR 0047), populated from the report filename at fetch; the migration backfills legacy rows from source_file before dropping it (AD-7).
- (decision) Open question: bronze retention as physical deletion/compaction vs write-time admission of broker-latest rows. Resolved by the architecture spine: retention is write-time Delta MERGE on the broker retention key (AD-1); matched rows replaced, absent keys untouched, VACUUM 7-day default per run (AD-3).
- (decision) Open question: issue #155 scope and the T212 handoff (retain optimization, replace with incremental event writes, or add per-layer filtered reads). Resolved by the architecture spine: single bronze read per broker run feeds both outputs (AD-6); the T212 in-memory handoff is removed as a measured experiment with a material-regression rollback against the ADR 0116 baseline (AD-8).
- (decision) Open question: whether to remove the T212 in-memory encrypted fetch handoff now that retention bounds bronze reads, or retain it to avoid one bounded read and its network roundtrip. Resolved by the architecture spine AD-8: remove the handoff for a measured simplification experiment; compare memory peak and runtime with the ADR 0116 baseline; restore it only if the regression is material.
- (constraint) The architecture spine ARCHITECTURE-SPINE.md (8 ADs, status final, owned by bmad-architecture) is an adopted companion: downstream skills cite its stable AD-1..AD-8 IDs; bmad-spec does not edit it. All four previously-open implementation questions are resolved by spine ADs (XTB key AD-2/AD-7, retention mechanism AD-1, issue #155/handoff AD-6/AD-8).
- (event) Self-validate pass 1 coherence after spine adoption: SPEC.md satisfies Spec Law 1-6 and 8 - every capability has intent+success, IDs stable (CAP-1..CAP-5), intents stay WHAT-level (mechanisms live in the spine companion), constraints bend design, non-goals explicit, success signal testable, lean. Open Questions section removed: all four prior open questions are resolved by spine ADs (memlog entries 50-55).
- (event) Self-validate pass 2 preservation after spine adoption: every load-bearing claim from the spine lands either in the kernel's constraints/assumptions or via the adopted ARCHITECTURE-SPINE.md companion; the four previously open questions are preserved as resolved decisions (entries 51-54); no wrapper content silently dropped.
- (decision) Story 5-4 deviates from the spine's AD-5 (run-aware freshness): the run-aware plumbing is redundant because the 5-2 merge write (AD-1) already bumps fetched_at on byte-identical re-fetches and transforms propagate raw fetched_at to silver, so issue #157 is fixed without any run-context threading. 5-4 becomes (a) a static per-account staleness check in quality.py (registry `ACCOUNT_STALENESS_KEYS`, no run context, ADR 0072 fallback intact, no new metadata table — directly implements the user's "oldest data" signal against the in-memory silver table) and (b) a manual `purge-account` escape hatch for legitimate full removal of one account's records (raw + snapshot + events; gold self-heals on next consolidate). Two user edge cases are binding: stale report → flag in freshness, never delete the snapshot (deleting breaks events-to-snapshot consistency: deposit 10k, trade 5k → net should be 5k + equity); all records of an account that must be legitimately removed → explicit purge.
