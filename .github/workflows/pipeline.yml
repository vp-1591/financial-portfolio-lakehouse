name: Pipeline

on:
  workflow_dispatch:
    inputs:
      command:
        description: "Pipeline command to run"
        required: true
        default: "full"
        type: choice
        options:
          - full
          - fetch
          - transform
          - consolidate
          - allocate
      target-currency:
        description: "Target currency for consolidation"
        required: false
        default: "EUR"
        type: string
      ibkr-enabled:
        description: "Enable IBKR connector"
        required: false
        default: true
        type: boolean
      t212-enabled:
        description: "Enable Trading 212 connector"
        required: false
        default: true
        type: boolean
      xtb-enabled:
        description: "Enable XTB connector"
        required: false
        default: true
        type: boolean
      demo:
        description: "Run in demo mode (uses _DEMO secrets and demo storage)"
        required: false
        default: false
        type: boolean

jobs:
  run:
    runs-on: ubuntu-latest

    env:
      DEMO: ${{ inputs.demo && 'true' || '' }}
      S3_BUCKET: ${{ vars.S3_BUCKET }}
      S3_PREFIX: ${{ vars.S3_PREFIX || 'pipeline' }}
      S3_BUCKET_DEMO: ${{ vars.S3_BUCKET_DEMO }}
      S3_PREFIX_DEMO: ${{ vars.S3_PREFIX_DEMO }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_ACCESS_KEY_ID_DEMO: ${{ secrets.AWS_ACCESS_KEY_ID_DEMO }}
      AWS_SECRET_ACCESS_KEY_DEMO: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEMO }}
      AWS_REGION: ${{ vars.AWS_REGION || 'eu-west-1' }}
      IBKR_FLEX_TOKEN: ${{ secrets.IBKR_FLEX_TOKEN }}
      IBKR_FLEX_QUERY_ID: ${{ secrets.IBKR_FLEX_QUERY_ID }}
      IBKR_FLEX_TOKEN_DEMO: ${{ secrets.IBKR_FLEX_TOKEN_DEMO }}
      IBKR_FLEX_QUERY_ID_DEMO: ${{ secrets.IBKR_FLEX_QUERY_ID_DEMO }}
      T212_API_KEY: ${{ secrets.T212_API_KEY }}
      T212_API_SECRET: ${{ secrets.T212_API_SECRET }}
      T212_API_KEY_DEMO: ${{ secrets.T212_API_KEY_DEMO }}
      T212_API_SECRET_DEMO: ${{ secrets.T212_API_SECRET_DEMO }}
      ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY }}
      ENCRYPTION_KEY_DEMO: ${{ secrets.ENCRYPTION_KEY_DEMO }}
      IBKR_ENABLED: ${{ inputs.ibkr-enabled }}
      T212_ENABLED: ${{ inputs.t212-enabled }}
      XTB_ENABLED: ${{ inputs.xtb-enabled }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[pipeline]"

      - name: Run pipeline
        run: python -m pipeline.run ${{ inputs.command }} --target-currency "${{ inputs.target-currency }}"